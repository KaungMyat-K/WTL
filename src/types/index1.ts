export type Language = {
  code: string;
  name: string;
  img: Image;
};

export type LinkItem = {
  label: string;
  href: string;
};

export type Image = {
  id: string;
  src: string;
  alt: string;
};
